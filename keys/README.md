# Keys directory

Place your hardware-generated `.key` files here.  
These are produced by `keygen/generate_keys.py` from the NinjiaTag project.

**Do not commit your key files** — this directory is listed in `.gitignore`.
